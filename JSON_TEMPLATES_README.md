# 📝 Система шаблонов на основе JSON

**Версия 2.0** - Упрощенная и легко редактируемая система управления шаблонами

---

## 🎯 Преимущества нового подхода

### ✅ Простота редактирования
- **JSON формат** - легко читать и редактировать
- **Не нужен Python** - можно редактировать в любом текстовом редакторе
- **Не нужно перезапускать app.py** - есть функция перезагрузки

### ✅ Прозрачность
- Все шаблоны в одном файле
- Видно приоритеты, действия, паттерны
- Легко добавлять комментарии

### ✅ Гибкость
- Легко добавлять новые шаблоны
- Легко менять приоритеты
- Легко редактировать паттерны

---

## 📁 Файлы системы

```
Test/
├── templates/
│   ├── error_templates_config.json  ← Конфигурация шаблонов (ГЛАВНЫЙ ФАЙЛ)
│   └── errors/
│       ├── idoc_zablokirovan.msg
│       ├── zakaz_v_systeme.msg
│       └── zamena_GLN Lenta.msg
├── letters_templates_v2.py          ← Новый менеджер шаблонов
└── app.py                            ← Обновить импорт
```

---

## 📄 Структура JSON конфигурации

### Общая структура

```json
{
  "templates": [
    { /* шаблон 1 */ },
    { /* шаблон 2 */ },
    ...
  ],
  "actions": {
    "block_and_notify": { /* настройки действия */ },
    ...
  },
  "config": {
    "msg_folder": "templates/errors",
    ...
  }
}
```

### Структура шаблона

```json
{
  "id": "duplicate_po_ruedigiper",
  "name": "Duplicate PO RUEDIGIPER",
  "description": "Duplicate PO RUEDIGIPER",
  "patterns": [
    "Duplicate",
    "PO",
    "ship-to",
    "RUEDIGIPER"
  ],
  "action": "block_and_notify",
  "msg_file": "idoc_zablokirovan.msg",
  "priority": 15,
  "comment": "Блокировать IDoc и оповестить CSA"
}
```

### Поля шаблона

| Поле | Обязательное | Описание |
|------|-------------|----------|
| `id` | ✅ Да | Уникальный идентификатор (латиница, цифры, подчеркивания) |
| `name` | ✅ Да | Полное название шаблона |
| `description` | ✅ Да | Короткое описание (отображается пользователю) |
| `patterns` | ✅ Да | Список ключевых слов для поиска |
| `action` | ✅ Да | Действие (`block_and_notify`, `push_and_notify`, и т.д.) |
| `msg_file` | ✅ Да | Имя MSG файла с шаблоном письма |
| `priority` | ⚠️ Рекомендуется | Приоритет (выше = важнее) по умолчанию 0 |
| `comment` | ❌ Нет | Комментарий для разработчиков |
| `alternative_patterns` | ❌ Нет | Альтернативные наборы паттернов (см. ниже) |

---

## 🔧 Как добавить новый шаблон

### Способ 1: Редактирование JSON (рекомендуется)

1. Откройте `templates/error_templates_config.json`

2. Добавьте новый шаблон в массив `templates`:

```json
{
  "id": "new_error_template",
  "name": "Новая ошибка",
  "description": "Новая ошибка",
  "patterns": [
    "ключевое",
    "слово",
    "код_клиента"
  ],
  "action": "block_and_notify",
  "msg_file": "idoc_zablokirovan.msg",
  "priority": 15,
  "comment": "Описание что делать"
}
```

3. Сохраните файл

4. Перезагрузите конфигурацию (без перезапуска app.py):
   - В Python: `template_manager.reload_config()`
   - Или просто перезапустите `app.py`

### Способ 2: Программное добавление

```python
from letters_templates_v2 import template_manager

template_manager.add_template(
    template_id="new_error_template",
    name="Новая ошибка",
    description="Новая ошибка",
    patterns=["ключевое", "слово", "код_клиента"],
    action="block_and_notify",
    msg_file="idoc_zablokirovan.msg",
    priority=15,
    comment="Описание что делать"
)
```

---

## 🎯 Приоритеты шаблонов

Приоритет влияет на выбор шаблона когда несколько шаблонов совпадают.

### Рекомендуемые значения

| Приоритет | Когда использовать | Примеры |
|-----------|-------------------|---------|
| **20+** | Очень специфичные запросы (много паттернов) | "RUEDIMAKSI + RU3A-01 + GLN" |
| **15** | Конкретные ошибки с кодом клиента | "Duplicate PO + RUEDIGIPER" |
| **10** | Общие ошибки с конкретными параметрами | "Function LF does not exist" |
| **8** | Общие ошибки | "Material is not defined" |
| **5** | Низкоприоритетные (для избежания ложных срабатываний) | "Аю" (GLN клиента) |

### Правило

**Чем БОЛЬШЕ число, тем ВЫШЕ приоритет.**

При одинаковом проценте совпадения паттернов выбирается шаблон с более высоким приоритетом.

---

## 🔍 Логика поиска шаблона

### Алгоритм

1. **Нормализация** запроса:
   - Удаление лишних пробелов
   - Замена `…` на `...`
   - Удаление запятых
   - Приведение к нижнему регистру

2. **Поиск паттернов** (ТОЛЬКО В ЗАПРОСЕ):
   - Проверяются ВСЕ паттерны из `patterns`
   - Паттерны из контекста RAG **ИГНОРИРУЮТСЯ**
   - Требуется 100% совпадение всех паттернов

3. **Вычисление балла**:
   ```
   Балл = (Процент совпадения × 100) + Приоритет
   ```

4. **Выбор лучшего**:
   - Выбирается шаблон с максимальным баллом
   - При равных баллах - первый в списке

### Пример

Запрос: `"Duplicate PO found for ship-to customer RUEDIGIPER"`

| Шаблон | Паттернов | Совпало | % | Приоритет | Балл | Выбран? |
|--------|-----------|---------|---|-----------|------|---------|
| Duplicate PO RUEDIMISHL | 4 | 3 | 75% | 15 | 90.0 | ❌ |
| Duplicate PO RUEDIGIPER | 4 | 4 | 100% | 15 | **115.0** | ✅ |

---

## 📋 Действия (Actions)

Доступные действия определены в разделе `actions` конфигурации:

```json
{
  "actions": {
    "block_and_notify": {
      "display_name": "Блокировать IDoc и оповестить CSA",
      "icon": "⚠️",
      "notify_csa": true
    },
    "block_no_notify": {
      "display_name": "Блокировать IDoc БЕЗ оповещения CSA",
      "icon": "⚠️",
      "notify_csa": false
    },
    "push_and_notify": {
      "display_name": "Протолкнуть IDoc и оповестить",
      "icon": "✅",
      "notify_csa": true
    },
    "lenta_gln_change": {
      "display_name": "Замена GLN для Ленты",
      "icon": "🏪",
      "notify_csa": true
    }
  }
}
```

### Добавление нового действия

1. Добавьте действие в раздел `actions`
2. Создайте соответствующий `.msg` файл
3. Обновите `app.py` для отображения (секция `action_info`)

---

## 🧪 Тестирование

### Запуск тестов

```bash
python letters_templates_v2.py
```

Это запустит встроенные тесты для проверки работы системы.

### Ручное тестирование

```python
from letters_templates_v2 import template_manager

# Поиск шаблона
result = template_manager.find_matching_template(
    query="Duplicate PO found for ship-to customer RUEDIGIPER",
    debug=True  # Включить подробный вывод
)

if result:
    print(f"Найден: {result['description']}")
    print(f"Действие: {result['action']}")
    print(f"MSG файл: {result['msg_file']}")
```

---

## 🔄 Обновление app.py

Чтобы использовать новую систему, обновите импорт в `app.py`:

### Старый код

```python
from letters_templates import template_manager
```

### Новый код

```python
# Используем новую версию на основе JSON
from letters_templates_v2 import template_manager
```

Все остальное работает без изменений!

---

## 📊 Примеры шаблонов

### Пример 1: Простой шаблон

```json
{
  "id": "material_not_defined",
  "name": "Material is not defined",
  "description": "Material is not defined",
  "patterns": [
    "Material",
    "is not defined"
  ],
  "action": "block_and_notify",
  "msg_file": "idoc_zablokirovan.msg",
  "priority": 8,
  "comment": "Блокировать IDoc и оповестить CSA"
}
```

### Пример 2: Специфичный шаблон с кодом клиента

```json
{
  "id": "duplicate_po_ruedigiper",
  "name": "Duplicate PO RUEDIGIPER",
  "description": "Duplicate PO RUEDIGIPER",
  "patterns": [
    "Duplicate",
    "PO",
    "ship-to",
    "RUEDIGIPER"
  ],
  "action": "block_and_notify",
  "msg_file": "idoc_zablokirovan.msg",
  "priority": 15,
  "comment": "Высокий приоритет - конкретный клиент"
}
```

### Пример 3: Шаблон с альтернативными паттернами

```json
{
  "id": "ayu_gln_xd03",
  "name": "Аю (GLN клиента / XD03)",
  "description": "Аю",
  "patterns": [
    "GLN",
    "клиента"
  ],
  "alternative_patterns": [
    ["XD03"]
  ],
  "action": "block_and_notify",
  "msg_file": "idoc_zablokirovan.msg",
  "priority": 5,
  "comment": "Низкий приоритет для избежания ложных срабатываний"
}
```

**Примечание:** `alternative_patterns` позволяет указать альтернативные наборы паттернов. Достаточно чтобы совпали либо все основные паттерны, либо один набор альтернативных.

---

## ⚠️ Важные правила

### 1. Уникальность ID

ID шаблона должен быть **уникальным**. Используйте формат:
- `error_type_client_code` - для ошибок с кодом клиента
- `error_type` - для общих ошибок
- Пример: `duplicate_po_ruedigiper`, `material_not_defined`

### 2. Паттерны в запросе

Система ищет паттерны **ТОЛЬКО в запросе пользователя**, а не в контексте RAG. Это предотвращает ложные срабатывания.

### 3. Приоритеты

Назначайте приоритеты разумно:
- **Высокий (15-20)**: для специфичных ошибок
- **Средний (8-10)**: для общих ошибок
- **Низкий (5 и ниже)**: для шаблонов которые могут ложно срабатывать

### 4. MSG файлы

Убедитесь что MSG файл существует в папке `templates/errors/`:
```bash
ls templates/errors/
# idoc_zablokirovan.msg
# zakaz_v_systeme.msg
# zamena_GLN Lenta.msg
```

---

## 🐛 Отладка

### Включить debug режим

```python
result = template_manager.find_matching_template(
    query="ваш запрос",
    debug=True  # ← Подробный вывод
)
```

Вы увидите:
- Нормализованный запрос
- Каждый проверяемый шаблон
- Количество совпадений
- Вычисленные баллы
- Выбранный шаблон

### Проверка конфигурации

```python
# Список всех шаблонов
templates = template_manager.list_templates()
for t in templates:
    print(f"{t['id']}: {t['name']} (priority={t['priority']})")

# Получить конкретный шаблон
template = template_manager.get_template_by_id("duplicate_po_ruedigiper")
print(template)
```

---

## 📝 Частые вопросы (FAQ)

### Q: Как перезагрузить конфигурацию без перезапуска app.py?

A: Вызовите `template_manager.reload_config()` в Python коде.

### Q: Почему мой шаблон не находится?

A: Проверьте:
1. Все ли паттерны присутствуют в запросе (включите `debug=True`)
2. Нет ли опечаток в паттернах
3. Не блокирует ли другой шаблон с более высоким приоритетом

### Q: Можно ли использовать regex в паттернах?

A: Нет, текущая версия использует простой поиск подстроки. Это сделано намеренно для простоты и надежности.

### Q: Как изменить порядок проверки шаблонов?

A: Измените приоритеты. Шаблоны проверяются от большего приоритета к меньшему.

### Q: Можно ли добавлять шаблоны "на лету" без редактирования JSON?

A: Да, используйте метод `template_manager.add_template()` (см. раздел "Способ 2" выше).

---

## 🚀 Миграция со старой версии

### Шаг 1: Сохраните старую версию

```bash
cp letters_templates.py letters_templates_old.py
```

### Шаг 2: Обновите app.py

Измените импорт:

```python
# Было
from letters_templates import template_manager

# Стало
from letters_templates_v2 import template_manager
```

### Шаг 3: Проверьте работу

```bash
python letters_templates_v2.py
```

### Шаг 4: Перезапустите app.py

```bash
python app.py
```

---

## ✅ Преимущества vs старая версия

| Критерий | Старая версия | Новая версия (JSON) |
|----------|--------------|---------------------|
| Формат конфигурации | Word .docx + Python код | JSON + Python код |
| Редактирование | Нужен Word + Python | Любой текстовый редактор |
| Добавление шаблона | Редактирование .docx + перезапуск | Редактирование JSON |
| Прозрачность | Низкая (логика в коде) | Высокая (все в JSON) |
| Приоритеты | В коде | В конфигурации |
| Отладка | Сложная | Простая (debug=True) |
| Производительность | Парсинг .docx при запуске | Быстрая загрузка JSON |
| Гибкость | Средняя | Высокая |

---

## 📞 Поддержка

Если возникнут вопросы или проблемы:

1. Проверьте логи с `debug=True`
2. Убедитесь что JSON файл корректный (можно проверить на jsonlint.com)
3. Проверьте что MSG файлы существуют
4. См. примеры в этом README

---

**Успешной работы с новой системой шаблонов!** 🎉
